# dépendances
node_modules/

# build
/dist
.quasar/
/.quasar
/quasar.config.*.temporary.compiled*

# scaffold temporaire
/.scaffold

# Cordova / Capacitor (si ajoutés plus tard)
/src-cordova/node_modules
/src-capacitor/node_modules
/src-capacitor/www

# env / secrets — ne jamais committer .env
.env
.env.*
!.env.example

# token Supabase (Personal Access Token) — jamais committé
.supabase-token

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# éditeur / OS
.DS_Store
Thumbs.db
*.log

# Packs 3D bruts (Quaternius CC0) : ~419 Mo de sources FBX + textures 4K.
# JAMAIS committes — on ne versionne que le bundle SLIM derive (public/hero/).
/public/models/
